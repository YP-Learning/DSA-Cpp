/*
# R-1.10 
What (if anything) is different about the behavior of the following two
functions f and g that increment a variable and print its value?
    void f(int x)     // does not affect the original variable
        { std::cout << ++x; }
    
    void g(int& x)   // will update the original variable too
        { std::cout << ++x; }

*/

int main(const int argc, const char** argv)
{
    return (0);
}
